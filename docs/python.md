SendRegning Python client
=========================

To install module, run the following command on the setup.py file

```python
python setup.py install
```

(Requires the module on this link: http://docs.python-requests.org/en/latest/)
